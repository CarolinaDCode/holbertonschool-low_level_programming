#include "holberton.h"
#include <string.h>

/**
 * _strlen - calculate the lenght of a string
 * @s: input string
 * Return: length
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
