#include <stdlib.h>
#include <time.h>
/* more headers goes there */
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int numero;

	for (numero = 48 ; numero <= 57 ; numero++)
	{
		putchar(numero);
	}
	putchar('\n');
	return (0);
}
